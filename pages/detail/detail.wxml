<!-- 单词卡片详情 -->
<view class="container">
  <view class="card-main">
    <!-- 单词头部 -->
    <view class="word-header">
      <view class="word-row">
        <text class="word-text">{{word.word}}</text>
        <view class="word-actions">
          <text class="action-fav {{isFav ? 'fav-active' : ''}}" bindtap="onToggleFav">
            {{isFav ? '❤️' : '🤍'}}
          </text>
          <text class="action-master {{isMastered ? 'master-active' : ''}}" bindtap="onToggleMaster">
            {{isMastered ? '✅' : '📌'}}
          </text>
        </view>
      </view>
      <text class="word-phonetic">{{word.phonetic}}</text>
      <view class="word-stars">
        <text class="stars">{{stars}}</text>
        <text class="freq-label">{{freqLabel}}</text>
      </view>
    </view>

    <!-- 释义 -->
    <view class="section">
      <view class="section-label">释义</view>
      <text class="cn-mean">{{word.cnMeaning}}</text>
      <text class="en-mean" wx:if="{{word.enMeaning}}">{{word.enMeaning}}</text>
    </view>

    <!-- 发音区域 -->
    <view class="section audio-section">
      <view class="section-label">发音</view>
      <view class="audio-list">
        <view class="audio-btn" bindtap="onPlayWord" data-type="word">
          <view class="audio-left">
            <text class="play-icon">{{playing === 'word' ? '🔊' : '▶️'}}</text>
            <text class="audio-text">单词</text>
          </view>
          <text class="audio-word">{{word.word}}</text>
        </view>

        <view class="audio-btn" bindtap="onPlayExample" data-type="example">
          <view class="audio-left">
            <text class="play-icon">{{playing === 'example' ? '🔊' : '▶️'}}</text>
            <text class="audio-text">课本例句</text>
            <text class="audio-badge tag tag-orange">课本</text>
          </view>
        </view>

        <view class="audio-btn" bindtap="onPlayExtra" data-type="extra">
          <view class="audio-left">
            <text class="play-icon">{{playing === 'extra' ? '🔊' : '▶️'}}</text>
            <text class="audio-text">拓展例句</text>
            <text class="audio-badge tag tag-blue">拓展</text>
          </view>
        </view>
      </view>
    </view>

    <!-- 课本例句 -->
    <view class="section" wx:if="{{example}}">
      <view class="section-label">
        <text class="tag tag-orange">课本例句</text>
      </view>
      <view class="example-box orange">
        <text class="example-en">{{example.sentence}}</text>
        <text class="example-cn">{{exampleCn}}</text>
      </view>
      <view class="example-scene" wx:if="{{example.scene}}">📌 {{example.scene}}</view>
    </view>

    <!-- 拓展例句 -->
    <view class="section" wx:if="{{extraExample}}">
      <view class="section-label">
        <text class="tag tag-blue">拓展例句</text>
      </view>
      <view class="example-box blue">
        <text class="example-en">{{extraExample}}</text>
        <text class="example-cn">{{extraCn}}</text>
      </view>
    </view>

    <!-- 小知识 -->
    <view class="section tip-section" wx:if="{{tip}}">
      <view class="section-label">
        <text class="tag tag-green">考点小知识</text>
      </view>
      <text class="tip-text">{{tip}}</text>
    </view>

    <!-- 来源 -->
    <view class="source-bar">
      <text>{{word.module}} · 外研版八年级下册</text>
    </view>
  </view>
</view>
